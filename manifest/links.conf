# Link records are:
# repository-relative-source|home-relative-destination
#
# Sources must be existing regular files inside the repository.
# Destinations must be relative paths inside HOME.
#
# Task 8 adds the locked link mappings after every source exists.
git/gitconfig.symlink|.gitconfig
git/gitconfig.local.example|.gitconfig.local.example
kitty/kitty.conf|.config/kitty/kitty.conf
powerlevel10k/p10k.zsh|.p10k.zsh
